import './Banner.css'

const Banner = () => {
    //JSX: JavaScript + XML (parece HTML, mas não é)
    return(
        <header className='banner'>
            <img src="/imagens/banner.png" alt="O banner principal da página do Organo" />
        </header>
    )
}

export default Banner;